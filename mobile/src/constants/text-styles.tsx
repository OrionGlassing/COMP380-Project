import { StyleSheet } from "react-native";

const textStyles = StyleSheet.create({
    standard: {
        fontSize: 20,
        textAlign: 'center',
        color: "#ffff",
    },
    
});

export default textStyles;